---
import ProTemplatesLandingPage from "../../../../../components/ProTemplatesLandingPage.astro";

export function getStaticPaths() {
  return ["admin", "saas", "crm", "analytics"].map((template) => ({ params: { template } }));
}

const { template } = Astro.params;
---

<ProTemplatesLandingPage initialPreview={template} />
